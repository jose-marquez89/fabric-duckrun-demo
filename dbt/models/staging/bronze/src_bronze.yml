version: 2

sources:
  - name: bronze_lakehouse
    tables:
      - name: collisions
        meta:
          plugin: duckrun
          format: csv
          location: "{{ env_var('TARGET_LH_PATH') }}/Files/nyc-open-data/collisions.csv"